# Astah Export Workspace

Store the Astah project as `fittrack-architecture.asta` in this folder after it
is created or refreshed in Astah.

Export generated PNG or SVG files to `../../architecture/` so diagram images
stay in the existing tracked architecture image folder.
